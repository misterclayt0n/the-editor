# the-editor pi bridge shim

This directory is only a compatibility shim so the extension still auto-loads in this repo.

The standalone package now lives at:

[`/Users/misterclayt0n/code/the-editor/the-editor-pi-extension`](/Users/misterclayt0n/code/the-editor/the-editor-pi-extension)

Install that package with:

```bash
pi install /Users/misterclayt0n/code/the-editor/the-editor-pi-extension
```
