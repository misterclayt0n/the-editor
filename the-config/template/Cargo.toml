[package]
name    = "the-config-user"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

# This crate is intended to be built via `the-editor config check/build`.
[dependencies]
the-default = "0.1.0"
the-lib     = "0.1.0"
